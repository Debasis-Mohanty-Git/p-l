{
  "name": "daily-pnl-tracker-root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd api && npm install && node node_modules/prisma/build/index.js generate"
  }
}
